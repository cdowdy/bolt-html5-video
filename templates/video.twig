{% import 'video-macros.twig' as attrib %}
{% spaceless %}
<video {% spaceless %}{{ attrib.vidID(  video_id ) }} {{ attrib.styleAttrib( class ) }}{{ attrib.attribBuild( attributes, preload, poster, widthHeight, fragment ) }}{% endspaceless %}>
{{ attrib.videoBodyBuild(videoSources, tracks, fragment) }}
</video>{% endspaceless %}
